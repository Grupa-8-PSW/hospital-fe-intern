import { BloodTypePipe } from './blood-type.pipe';

describe('BloodTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BloodTypePipe();
    expect(pipe).toBeTruthy();
  });
});
